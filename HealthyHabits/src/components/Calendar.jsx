import React, { useState } from 'react';
import { StyleSheet, Text, View } from 'react-native';

const Calendar = ({styles}) => {
    return (
        <View style={styles.container}>
            <Text>I AM FROM CALENDAR</Text>
        </View>
    )
}

export default Calendar