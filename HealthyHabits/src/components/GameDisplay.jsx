import React, { useState } from 'react';
import { StyleSheet, Text, View } from 'react-native';

const GameDisplay = ({styles}) => {
    return (
        <View style={styles.container}>
            <Text>I AM FROM GAME DISPLAY</Text>
        </View>
    )
}

export default GameDisplay